{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'un événement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('workshop_show', { 'id': event.worOid.id }) }}">Retour à l'atelier</a>
        </li>
    </ul>
{% endblock %}
